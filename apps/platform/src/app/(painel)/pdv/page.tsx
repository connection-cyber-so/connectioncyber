import{PosOverview}from'@/features/sales/components/PosOverview';import{listOpenCashSessions,listPaymentMethods}from'@/features/sales/service';import{createClient}from'@/lib/supabase/server';import{requireCurrentTenantId}from'@/lib/tenant';
export default async function PosPage(){const c=await createClient(),t=await requireCurrentTenantId();try{const[sessions,methods]=await Promise.all([listOpenCashSessions(c,t),listPaymentMethods(c,t)]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">PDV online</p><h1>Caixa e pagamentos</h1><p>Operação transacional; contingência offline será tratada no M12.</p></div></div><PosOverview sessions={sessions} methods={methods}/></>}catch{return <div className="pf-content-card"><h1>Caixa e pagamentos</h1><p className="pf-notice">PDV preparado. A estrutura será habilitada após o portão da migration 0023.</p></div>}}
