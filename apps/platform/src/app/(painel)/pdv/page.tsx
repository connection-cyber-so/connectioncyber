import{LocalCashForm,LocalCloseCashForm,LocalSaleForm}from'@/features/sales/components/LocalPosForms';import{listLocalCash,listLocalCatalogItems,listLocalParties,listLocalSales,localPersistenceMode}from'@/features/persistence/local';
export const dynamic='force-dynamic';export default async function PosPage(){const[items,customers,cash,sales]=await Promise.all([listLocalCatalogItems(),listLocalParties(),listLocalCash(),listLocalSales()]);return<><div className="pf-page-header"><div><p className="pf-eyebrow">M18-G4</p><h1>PDV e caixa</h1><p>Preço, estoque, recebível e esperado de caixa derivados no servidor local.</p></div></div><p className="pf-notice"><strong>{localPersistenceMode}.</strong> Nenhuma venda possui valor fiscal.</p><div className="pf-grid-2">{cash?.status==='open'?<LocalCloseCashForm cash={cash}/>:<LocalCashForm/>}<LocalSaleForm items={items.filter(item=>item.track_inventory)} customers={customers.filter(row=>row.erp_party_roles.some(role=>role.role==='customer'))}/></div><div className="pf-grid-2"><section className="pf-content-card"><h2>Caixa atual</h2>{cash?<><p><strong>{cash.status}</strong></p><p>Fundo: R$ {cash.openingAmount.toFixed(2)}</p><p>Esperado: R$ {cash.expectedAmount.toFixed(2)}</p></>:<p>Nenhum caixa aberto.</p>}</section><section className="pf-content-card"><h2>Vendas da sessão</h2>{!sales.length?<p>Nenhuma venda.</p>:sales.map(row=><p key={row.id}><strong>{row.code}</strong> — {row.itemName} × {row.quantity} — R$ {row.total.toFixed(2)} · {row.paymentKind}</p>)}</section></div></>}
