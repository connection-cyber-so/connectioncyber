import{BankingOverview}from'@/features/finance/components/BankingOverview';import{listBankAccounts,listReconciliations}from'@/features/finance/service';import{createClient}from'@/lib/supabase/server';import{requireCurrentTenantId}from'@/lib/tenant';
export default async function BankingPage(){const c=await createClient(),t=await requireCurrentTenantId();try{const[accounts,reconciliations]=await Promise.all([listBankAccounts(c,t),listReconciliations(c,t)]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">Tesouraria</p><h1>Bancos e conciliação</h1><p>Extratos idempotentes e correspondência auditável.</p></div></div><BankingOverview accounts={accounts} reconciliations={reconciliations}/></>}catch{return <div className="pf-content-card"><h1>Bancos e conciliação</h1><p className="pf-notice">Módulo preparado. A estrutura será habilitada após o portão da migration 0024.</p></div>}}
