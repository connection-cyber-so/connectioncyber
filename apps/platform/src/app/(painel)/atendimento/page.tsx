import{SupportOverview}from'@/features/support/components/SupportOverview';import{listManagedDevices,listRemoteSessions,listSupportTickets}from'@/features/support/service';import{createClient}from'@/lib/supabase/server';import{requireCurrentTenantId}from'@/lib/tenant';
export const dynamic='force-dynamic';
export default async function SupportPage(){const c=await createClient(),t=await requireCurrentTenantId();try{const[tickets,devices,sessions]=await Promise.all([listSupportTickets(c,t),listManagedDevices(c,t),listRemoteSessions(c,t)]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">M11</p><h1>Atendimento e acesso remoto</h1><p>Tickets, SLA, dispositivos, consentimentos e sessões auditadas por empresa.</p></div></div><SupportOverview tickets={tickets} devices={devices} sessions={sessions}/></>}catch{return <div className="pf-content-card"><h1>Atendimento e acesso remoto</h1><p className="pf-notice">Módulo preparado. A estrutura será habilitada após o portão da migration 0027.</p></div>}}
