import{OperationsOverview}from'@/features/operations/components/OperationsOverview';import{listPriceLists,listPurchaseOrders,listStockLocations}from'@/features/operations/service';import{createClient}from'@/lib/supabase/server';import{requireCurrentTenantId}from'@/lib/tenant';
export default async function OperationsPage(){const c=await createClient(),t=await requireCurrentTenantId();try{const[prices,locations,orders]=await Promise.all([listPriceLists(c,t),listStockLocations(c,t),listPurchaseOrders(c,t)]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">M06</p><h1>Preços, estoque e compras</h1><p>Visão operacional isolada por empresa e estabelecimento.</p></div></div><OperationsOverview prices={prices} locations={locations} orders={orders}/></>}catch{return <div className="pf-content-card"><h1>Preços, estoque e compras</h1><p className="pf-notice">Módulo preparado. A estrutura de staging será habilitada após o portão da migration 0022.</p></div>}}
