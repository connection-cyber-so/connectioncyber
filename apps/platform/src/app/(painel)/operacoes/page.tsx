import{LocalInventoryForm}from'@/features/operations/components/LocalInventoryForm';import{listVisualCatalogItems,listVisualStock,visualPersistenceMode}from'@/features/persistence/selected';
export const dynamic='force-dynamic';export default async function OperationsPage(){const[items,stock]=await Promise.all([listVisualCatalogItems(),listVisualStock()]);const tracked=items.filter(item=>item.track_inventory);return<><div className="pf-page-header"><div><p className="pf-eyebrow">M18-G11</p><h1>Estoque</h1><p>Recebimento pela fronteira server-side com saldo relido após a operação.</p></div></div><p className="pf-notice"><strong>{visualPersistenceMode}.</strong> Os saldos desaparecem ao reiniciar o servidor.</p><LocalInventoryForm items={tracked}/><section className="pf-content-card"><h2>Saldos atuais</h2>{!stock.length?<p>Nenhum produto com estoque.</p>:stock.map(row=><p key={row.itemId}><strong>{row.code} — {row.name}</strong>: {row.quantity} UN</p>)}</section></>}
