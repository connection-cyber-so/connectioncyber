import { listVisualParties,listVisualPartyDocuments,listVisualPartyContacts,listVisualPartyAddresses,visualPersistenceMode,isWritePersistenceEnabled } from '@/features/persistence/selected';import { PartyForm } from '@/features/parties/components/PartyForm';import { PartyList } from '@/features/parties/components/PartyList';
export const dynamic='force-dynamic';export default async function CadastrosPage(){const[parties,documents,contacts,addresses]=await Promise.all([listVisualParties(),listVisualPartyDocuments(),listVisualPartyContacts(),listVisualPartyAddresses()]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">M18-G11 · M20-G3</p><h1>Cadastros</h1><p>Clientes, fornecedores, compradores, funcionários e demais parceiros sem duplicação — dados, documentos, contatos e endereços.</p></div></div><p className="pf-notice"><strong>{visualPersistenceMode}.</strong> {isWritePersistenceEnabled ? 'Os registros gravam de verdade no Supabase.' : 'Os registros desaparecem ao reiniciar o servidor.'}</p><PartyForm/><PartyList parties={parties} documents={documents} contacts={contacts} addresses={addresses}/></>}
