import{listVisualCatalogItems,listVisualUnits,visualPersistenceMode}from'@/features/persistence/selected';import{ItemForm}from'@/features/catalog/components/CatalogForms';import{CatalogList}from'@/features/catalog/components/CatalogList';
export const dynamic='force-dynamic';export default async function CatalogoPage(){const[units,items]=await Promise.all([listVisualUnits(),listVisualCatalogItems()]);return <><div className="pf-page-header"><div><p className="pf-eyebrow">M18-G11</p><h1>Catálogo universal</h1><p>Produtos, serviços, peças, ingredientes, preparados, kits, insumos, taxas e vales.</p></div></div><p className="pf-notice"><strong>{visualPersistenceMode}.</strong> Unidade sintética UN disponível; os itens desaparecem ao reiniciar o servidor.</p><ItemForm units={units}/><CatalogList items={items} units={units}/></>}
