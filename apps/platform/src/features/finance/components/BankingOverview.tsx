import type{BankAccount,BankReconciliation}from'../types';
export function BankingOverview({accounts,reconciliations}:{accounts:BankAccount[];reconciliations:BankReconciliation[]}){return <div className="pf-grid-2"><section className="pf-content-card"><h2>Contas bancárias</h2><p>{accounts.length} cadastrada(s)</p>{accounts.map(x=><p key={x.id}><strong>{x.bank_code}</strong> — {x.account_masked} — {x.erp_financial_accounts?.name??'Conta financeira'}</p>)}</section><section className="pf-content-card"><h2>Conciliações</h2><p>{reconciliations.length} recente(s)</p>{reconciliations.map(x=><p key={x.id}><strong>{x.period_from} a {x.period_to}</strong> — {x.status}</p>)}</section><section className="pf-content-card"><h2>Segurança bancária</h2><p>O sistema guarda somente dados mascarados e referências de segredo. Tokens, certificados e senhas permanecem em cofre externo.</p></section></div>}
