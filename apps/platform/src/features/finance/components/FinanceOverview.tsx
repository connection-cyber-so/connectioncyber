import type{FinancialAccount,FinancialEntry}from'../types';
export function FinanceOverview({entries,accounts}:{entries:FinancialEntry[];accounts:FinancialAccount[]}){const receive=entries.filter(x=>x.direction==='receivable'),pay=entries.filter(x=>x.direction==='payable');return <div className="pf-grid-3"><section className="pf-content-card"><h2>Contas a receber</h2><p>{receive.length} título(s)</p>{receive.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.status} — {x.currency_code} {Number(x.principal_amount).toFixed(2)}</p>)}</section><section className="pf-content-card"><h2>Contas a pagar</h2><p>{pay.length} título(s)</p>{pay.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.status} — {x.currency_code} {Number(x.principal_amount).toFixed(2)}</p>)}</section><section className="pf-content-card"><h2>Contas financeiras</h2><p>{accounts.length} ativa(s)</p>{accounts.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.name}</p>)}</section></div>}
