'use client';
import { useFormState,useFormStatus } from 'react-dom';import { createPartyAction,type PartyActionState } from '../actions';
const initial:PartyActionState={error:null,success:false};function Submit(){const{pending}=useFormStatus();return <button className="pf-button" type="submit" disabled={pending}>{pending?'Salvando…':'Cadastrar'}</button>}
export function PartyForm(){const[state,action]=useFormState(createPartyAction,initial);return <div className="pf-content-card"><h2>Novo cadastro</h2><form action={action}><div className="pf-grid-2"><div className="pf-field"><label htmlFor="kind">Tipo</label><select id="kind" name="kind" defaultValue="organization"><option value="organization">Pessoa jurídica</option><option value="person">Pessoa física</option></select></div><div className="pf-field"><label htmlFor="role">Papel</label><select id="role" name="role" defaultValue="customer"><option value="customer">Cliente</option><option value="supplier">Fornecedor</option><option value="buyer">Comprador</option><option value="employee">Funcionário</option><option value="sales_rep">Vendedor</option><option value="technician">Técnico</option><option value="carrier">Transportadora</option><option value="other">Outro</option></select></div></div><div className="pf-field"><label htmlFor="legal_name">Nome/Razão social</label><input id="legal_name" name="legal_name" required maxLength={180}/></div><div className="pf-grid-2"><div className="pf-field"><label htmlFor="trade_name">Nome fantasia</label><input id="trade_name" name="trade_name" maxLength={180}/></div><div className="pf-field"><label htmlFor="tax_id">CPF/CNPJ</label><input id="tax_id" name="tax_id" inputMode="numeric"/></div></div>{state.error&&<p className="pf-error">{state.error}</p>}{state.success&&<p className="pf-success">Cadastro criado.</p>}<Submit/></form></div>}
