import type{PriceList,PurchaseOrder,StockLocation}from'../types';
export function OperationsOverview({prices,locations,orders}:{prices:PriceList[];locations:StockLocation[];orders:PurchaseOrder[]}){return <div className="pf-grid-3"><section className="pf-content-card"><h2>Listas de preços</h2><p>{prices.length} configurada(s)</p>{prices.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.name} ({x.currency_code})</p>)}</section><section className="pf-content-card"><h2>Locais de estoque</h2><p>{locations.length} ativo(s)</p>{locations.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.name}</p>)}</section><section className="pf-content-card"><h2>Pedidos de compra</h2><p>{orders.length} recente(s)</p>{orders.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.status} — {x.currency_code} {Number(x.grand_total).toFixed(2)}</p>)}</section></div>}
