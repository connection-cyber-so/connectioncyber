'use server';import{revalidatePath}from'next/cache';import{z}from'zod';import{localPersistenceClient,prepareLocalSale}from'@/features/persistence/local';
export type PosState={error:string|null;success:string|null};const cashSchema=z.object({openingAmount:z.coerce.number().nonnegative().max(1000000)}),saleSchema=z.object({itemId:z.string().uuid(),quantity:z.coerce.number().int().positive().max(10000),paymentKind:z.literal('cash')});
export async function openCashAction(_:PosState,formData:FormData):Promise<PosState>{const parsed=cashSchema.safeParse({openingAmount:formData.get('openingAmount')});if(!parsed.success)return{error:'Informe um valor inicial válido.',success:null};const result=await localPersistenceClient.execute('cash.open',{cashRegisterId:'00000000-0000-4000-8000-000000000004',openingAmount:parsed.data.openingAmount});if(!result.ok)return{error:result.error.message,success:null};revalidatePath('/pdv');return{error:null,success:'Caixa aberto na sessão local.'};}
export async function completeSaleAction(_:PosState,formData:FormData):Promise<PosState>{const parsed=saleSchema.safeParse({itemId:formData.get('itemId'),quantity:formData.get('quantity'),paymentKind:formData.get('paymentKind')});if(!parsed.success)return{error:'Informe produto, quantidade e pagamento válidos.',success:null};try{const draft=await prepareLocalSale(parsed.data.itemId,parsed.data.quantity,parsed.data.paymentKind);const result=await localPersistenceClient.execute('sale.complete',{saleId:draft.id,saleIdempotencyKey:draft.idempotencyKey});if(!result.ok)return{error:result.error.message,success:null};revalidatePath('/pdv');revalidatePath('/operacoes');return{error:null,success:`Venda concluída. Total derivado no servidor: R$ ${draft.total.toFixed(2)}.`};}catch{return{error:'Não foi possível preparar a venda.',success:null}}}
