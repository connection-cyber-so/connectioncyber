import type{CashSession,PaymentMethod}from'../types';
export function PosOverview({sessions,methods}:{sessions:CashSession[];methods:PaymentMethod[]}){return <div className="pf-grid-2"><section className="pf-content-card"><h2>Caixas em operação</h2><p>{sessions.length} sessão(ões)</p>{sessions.map(x=><p key={x.id}><strong>{x.erp_cash_registers?.code??'Caixa'}</strong> — {x.status}</p>)}</section><section className="pf-content-card"><h2>Formas de pagamento</h2><p>{methods.length} ativa(s)</p>{methods.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.name}</p>)}</section><section className="pf-content-card"><h2>Fechamento protegido</h2><p>Preço recalculado no servidor, pagamento conciliado e baixa de estoque/caixa atômica.</p><p className="pf-notice">Operações reais permanecem desabilitadas até a validação remota da migration 0023.</p></section></div>}
