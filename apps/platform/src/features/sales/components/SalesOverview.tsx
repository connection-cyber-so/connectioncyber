import type{Quote,Sale}from'../types';
export function SalesOverview({quotes,sales}:{quotes:Quote[];sales:Sale[]}){return <div className="pf-grid-2"><section className="pf-content-card"><h2>Orçamentos recentes</h2><p>{quotes.length} registro(s)</p>{quotes.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.status} — {x.currency_code} {Number(x.grand_total).toFixed(2)}</p>)}</section><section className="pf-content-card"><h2>Vendas recentes</h2><p>{sales.length} registro(s)</p>{sales.map(x=><p key={x.id}><strong>{x.code}</strong> — {x.status} — {x.currency_code} {Number(x.grand_total).toFixed(2)}</p>)}</section></div>}
