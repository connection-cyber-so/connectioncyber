import{createVisualPersistenceClient}from'../src/server-client.mjs';const calls=[];const client=createVisualPersistenceClient({resolveTenant:async()=>'00000000-0000-4000-8000-000000000018',createRequestId:()=> 'M18-G1-SIMULATION-0001',transport:{async rpc(rpc,args){calls.push({rpc,tenant:args.p_tenant_id});return{id:'SYNTHETIC'};},async read(contract,tenant){calls.push({read:contract.source,tenant});return[];}}});const result=await client.execute('sale.complete',{saleId:'SYNTHETIC'});if(!result.ok||!result.revalidated)throw new Error('M18_G1_CLIENT_INVALID');console.log(JSON.stringify({result:'M18_G1_TYPED_SERVER_CLIENT_OK',rpcCalls:calls.filter(x=>x.rpc).length,readCalls:calls.filter(x=>x.read).length,tenantResolvedServerSide:true,remoteAccessed:false,productionAccessed:false}));
