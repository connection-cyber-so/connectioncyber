export class NFeSoapError extends Error{constructor(code){super(code);this.name='NFeSoapError';this.code=code}}
const fail=code=>{throw new NFeSoapError(code)};
export function resolveAuthorizer(catalog,uf){if(catalog.environment!=='homologation'||catalog.networkEnabled!==false||catalog.productionEnabled!==false)fail('UNSAFE_CATALOG');for(const[name,states]of Object.entries(catalog.routes))if(states.includes(uf))return name;fail('UF_NOT_ROUTED')}
export function buildSoap12Envelope({service,correlationId,payloadXml}){if(!/^NFe(?:Autorizacao4|RetAutorizacao4|ConsultaProtocolo4|StatusServico4)$/.test(service)||!/^corr-[a-z0-9-]{8,64}$/.test(correlationId)||typeof payloadXml!=='string'||payloadXml.length<10||/<(?:CNPJ|CPF)>/.test(payloadXml))fail('INVALID_SOAP_REQUEST');return`<soap12:Envelope xmlns:soap12="http://www.w3.org/2003/05/soap-envelope"><soap12:Header><correlationId>${correlationId}</correlationId><service>${service}</service></soap12:Header><soap12:Body>${payloadXml}</soap12:Body></soap12:Envelope>`}
export function createBlockedNFeSoapClient({catalog,transport,tls}){if(catalog.networkEnabled!==false||transport!==null||tls?.mode!=='mutual-tls-reference-only'||tls.privateKeyExportable!==false)fail('FAIL_CLOSED_CONFIGURATION_REQUIRED');return Object.freeze({networkEnabled:false,timeoutMs:15000,maxAttempts:1,send(){fail('NETWORK_GATE_CLOSED')}})}
